{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://typewriter.neverworkalone.dev/schema/m5-10a-wave-a2-editorial-decisions.schema.json",
  "title": "Typewriter M5-10A Wave A2 supplied editorial decisions",
  "description": "A separately authored decision artifact consumed by the editorial recorder. It is not a completed editorial claim.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "artifact_id",
    "batch_id",
    "source_kind",
    "actor_kind",
    "actor_id",
    "session_id",
    "created_at",
    "finalized_at",
    "proposal_staging_sha256",
    "reviewed_staging_sha256",
    "records",
    "note"
  ],
  "properties": {
    "schema_version": { "const": "1" },
    "artifact_id": { "type": "string", "pattern": "^m5-10a-wave-a2-editorial-decisions-[0-9]{8}$" },
    "batch_id": { "const": "m5-10-wave-a2-20260909" },
    "source_kind": { "enum": ["human-authored", "codex-authored"] },
    "actor_kind": { "enum": ["human", "codex"] },
    "actor_id": { "type": "string", "pattern": "\\S" },
    "session_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "created_at": { "type": "string", "format": "date-time" },
    "finalized_at": { "type": "string", "format": "date-time" },
    "proposal_staging_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "reviewed_staging_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "records": {
      "type": "array",
      "minItems": 58,
      "maxItems": 58,
      "items": { "type": "object" }
    },
    "note": { "type": "string", "pattern": "\\S" }
  }
}
